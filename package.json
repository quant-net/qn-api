{
  "name": "quant-net-api",
  "version": "1.0.0",
  "private": true,
  "description": "QuantNet API Dashboard",
  "scripts": {
    "build": "esbuild static/js/main.js --bundle --minify --sourcemap --outfile=static/js/dist/bundle.min.js --format=iife",
    "watch": "esbuild static/js/main.js --bundle --sourcemap --outfile=static/js/dist/bundle.js --format=iife --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
